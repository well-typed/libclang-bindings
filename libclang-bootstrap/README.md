# `libclang-bootstrap`

`libclang-bootstrap` is used to generate C wrapper functions and Haskell
foreign import declarations for a subset of the `libclang` C API. It is a
support package for `libclang-bindings`.
